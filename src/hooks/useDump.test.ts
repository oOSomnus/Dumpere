import { describe, it, expect } from 'vitest'

// Stub tests - will be implemented when useDump hook is created
describe('useDump', () => {
  it('should be implemented', () => {
    expect(true).toBe(true)
  })
})
